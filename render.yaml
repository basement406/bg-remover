services:
  - type: web
    name: bg-remover
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    # Increase timeout to 30 minutes
    healthCheckPath: /
    instanceCount: 1
